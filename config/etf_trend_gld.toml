# ETF Trend Strategy -- GLD locked config
# Generated by run_stage4_sizing.py
#
# *** V3.6 BULK-RETIRE (HIGH confidence, spot-check confirmed, 2026-05-16) ***
# Wave A.2-followup spot-check (4-cell audit on 21y GLD data):
#   - Plateau spread 43% (FAILS L27)
#   - All cells CI_lo NEGATIVE (-0.12 to -0.23)
#   - Rel-MC FAILS for every cell (median DD reduction ~0.93 vs B&H GLD)
#   - All cells SUSPECT or TIER_UNCONFIRMED
# NOTE: bond_gold V3.6 uses IEF→GLD CROSS-ASSET momentum (positive signal,
# PROMOTED CONDITIONAL). This GLD trend strategy uses GLD's OWN MA-
# crossover (different mechanism, no signal). Different mechanism, both
# correctly classified by V3.6 audits.
# Recommend de-allocate.
# See `.tmp/reports/etf_trend_gld_spotcheck/result_log.md`.

instrument        = "GLD.ARCA"
ma_type           = "SMA"
slow_ma           = 250
decel_signals     = []
entry_mode        = "asymmetric"
fast_reentry_ma   = 100
exit_mode         = "D"
exit_decel_thresh = -0.1
exit_confirm_days = 5
decel_confirm_days = 1
sizing_mode       = "binary"
atr_stop_mult     = 3.0
eod_eval_time     = "20:30"  # 15:30 ET in UTC
warmup_bars       = 300

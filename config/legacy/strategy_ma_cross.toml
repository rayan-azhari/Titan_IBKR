# ──────────────────────────────────────────────────────────
# strategy_ma_cross.toml — Moving average crossover parameters
# ──────────────────────────────────────────────────────────
# Referenced by ensemble.toml → MA_Crossover strategy.
# Parameters to be refined via VectorBT optimisation.

instrument = "EUR_USD"
granularity = "H4"

[ma]
fast_period = 20
slow_period = 50

[costs]
spread = 0.0003
commission_per_lot = 0.0

[execution]
position_size_units = 5000
